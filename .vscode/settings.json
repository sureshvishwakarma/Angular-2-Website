// Place your settings in this file to overwrite default and user settings.
{
    "editor.fontSize": 14,
    
     "files.exclude": {
        "**/*.map": {},
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.css": {
            "when": "$(basename).scss"
        }
    }
     
    
}


